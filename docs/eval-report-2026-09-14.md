# 检索/答案级评测报告 · 2026-09-14

> 语料：8 篇文档 / 5750 切片（已去重）　评测集：8 条标注用例　K=5
> 环境：deepseek-flash（评测中途账户余额耗尽，见「阻塞项」）

## 一、结论摘要

| # | 结论 | 证据强度 |
|---|---|---|
| 1 | **Agent 在质量上明显优于管线**（correctness 0.61→0.73，忠实度 0.93→0.99，未支撑论断 5→1） | 中：n=8，判官有噪声 |
| 2 | **改写通道的增益无法与噪声区分**（三次测量 ΔMRR 从 −0.006 到 +0.135） | 强：三次独立测量互相矛盾 |
| 3 | **规划类通道（分解/退步/HyDE）引入不可复现性** | 强：同配置两次运行结果不同 |
| 4 | **结构化引用 + 同义词确实有效** | 强：端到端答案正确性 0.00 → 1.00 |
| 5 | **8 条评测例分辨率不足**：1 例翻转 = 0.125 hit@5，任何 <0.1 的差异不可判读 | 强：由 2、3 直接推出 |

## 二、实验一：完整配置消融（首轮，含规划）

| 配置 | hit@5 | MRR | 逐例命中 (1..8) |
|---|---|---|---|
| none(纯向量) | 0.625 | 0.4000 | ✓✗✓✓✓✗✓✗ |
| default(含改写) | 0.750 | **0.6667** | ✓✓✓✓✗✓✓✗ |
| default-改写(关) | 0.750 | 0.5312 | ✓✓✓✓✗✓✓✗ |
| +graph | 0.750 | 0.4479 | ✓✓✓✓✗✓✓✗ |
| +graph+rerank | 0.750 | 0.5938 | ✓✓✓✓✗✓✓✗ |
| all(+rerank:rrf) | 0.750 | 0.7500 | ✓✓✓✓✗✓✓✗ |

**表面结论**：改写 +0.135 MRR。**但这个结论被实验二推翻。**

## 三、实验二：确定性配置双轮（无 LLM 参与）

规划类特性不开时，全流程无 LLM 调用，可完全复现：

| 配置 | hit@5 (R1/R2) | MRR (R1/R2) | 两轮一致 |
|---|---|---|---|
| none(纯向量) | 0.625 / 0.625 | 0.4000 / 0.4000 | 是 |
| routing+keywords | 0.750 / 0.750 | 0.5104 / 0.5104 | 是 |
| routing+keywords+**rewrite** | 0.750 / 0.750 | **0.5042 / 0.5042** | 是 |
| routing+keywords+rewrite+graph | 0.750 / 0.750 | 0.5250 / 0.5250 | 是 |

- **两轮完全一致** → 确定性配置可复现；方差只可能来自 LLM 通道。
- **加改写后 MRR 0.5104 → 0.5042（−0.006）**，hit@5 不变 → 改写无正向贡献。

## 四、发现一：规划通道破坏可复现性

同一条查询（#5「什么是 FastAPI？它有什么特点？」）在同一配置下：

- 首轮消融的 `default(含改写)`：**未命中**
- 手工单跑同一配置：**命中**
- 首轮消融的 `none(纯向量)`：**命中**

唯一差异是规划类特性（decompose / stepback / hyde）会调 LLM 生成子查询、退步问题、假想段落，
这些内容每次略有不同 → 检索候选集变化 → 命中结果翻转。

**这对「评测平台」的立身之本是真问题**：只要默认集里有规划类特性，两次消融就不可比。
补救方向（择一）：
1. 消融时把 `plan_question` 的输出缓存/固定（同一条用例复用同一份计划）；
2. 规划类特性在评测中单独成行，与确定性基线分开报告；
3. 记录每次运行的 plan 输出，随结果一起落库，便于审计。

## 五、发现二：改写通道的增益无法与噪声区分

`rewrite_query` 把口语问句压成紧凑检索串（去疑问后缀、归一法条编号、拼实体词）作为额外一路通道。
确定性配置下：hit@5 不变（0.75 → 0.75），MRR 略降（0.5104 → 0.5042）。

补充第三轮测量（规划生效、LLM 恢复后）后，三次独立测量的 ΔMRR 落在 **−0.006 ~ +0.135**：
详见 §8.3。由于 8 例评测集里 1 例翻转就值 0.125 hit@5，**这些差异全部落在噪声带内**。

**建议**：暂不下「移出默认集」的结论——因为**没有证据说明它有害，也没有证据说明它有用**。
正确做法是**先扩样到 ≥50 条再判**。在此之前调整默认开关都等于拍脑袋，
这正是 `tool_call` 事故（分解/退步/HyDE 长期空转却显示"无需拆分"）给的教训。

> 注意区分：#6 的收益来自**结构化引用抽取 + 同义词映射**（关键词通道与过滤层），与改写通道无关——
> 两者经常被混为一谈，实证上必须分开看。

## 六、发现三：结构化引用 + 同义词确实有效

「第4页的插图是什么？」修复前后（答案级判官，同一套用例）：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| #6 正确性 | 0.00 | **1.00** |
| 聚合 correctness（前 3 例） | 0.633 | **0.94** |
| 聚合 faithfulness（前 3 例） | 0.936 | **1.00** |
| 未支撑论断 | 2 | **0** |

端到端从「返回《傲慢与偏见》的插图页」变为「唯一返回 `RAG.pdf#158`」。

## 七、阻塞项：LLM 余额耗尽（已解决）

跑全量答案级评测（8 例 × pipeline + agent）时中断：

```
openai.APIStatusError: Error code: 402 - {'error': {'message': 'Insufficient Balance'}}
```

更换 provider 后已恢复，全量结果见第八节。顺带修掉一个健壮性缺陷：单例生成失败原先会让整轮评测崩溃，
现在改为记录失败、继续跑其他用例，并在汇总里输出失败数（`失败=N`）。

## 八、全量结果（LLM 恢复后补跑，8 例）

### 8.1 答案级评测：Agent 明显优于管线

| 配置 | faithfulness | correctness | 未支撑论断 |
|---|---|---|---|
| pipeline(含改写) | 0.9313 | 0.6125 | 5 |
| **agent(steps=4)** | **0.9904** | **0.7313** | **1** |

逐例正确性（pipeline → agent）：

| 用例 | 类型 | pipeline | agent |
|---|---|---|---|
| #5 | FastAPI 定义 | 0.95 | 0.95 |
| #6 | 第4页插图 | 0.95 | 1.00 |
| #7 | ReCite CiteLocator 消融（多跳）| 0.50 | **0.90** |
| #8 | CAP-8 + Strict F1 | 1.00 | 0.90 |
| #9 | QueryPlanner 消融（多跳）| 0.50 | 0.40 |
| #10 | ReCite 设计思路 + 对比基线（多跳）| 0.20 | **0.40** |
| #11 | FastAPI 最小示例 | 0.80 | 0.80 |
| #12 | 两套文档网址 | 0.00 | **0.50** |

**这修正了本报告初稿（及更早口头结论）的判断。** 早前基于 n=1（#7 那道多跳题）说
「Agent 基本不值得」——本次同一道 #7，agent 0.90 vs pipeline 0.50。样本从 1 扩到 8 后，
Agent 在**正确性（+0.12）、忠实度（+0.06）、幻觉（未支撑论断 5→1）**三项全面更好。

代价仍然存在：agent 每例 4 步、多次 LLM 调用与检索，延迟与成本显著更高。
结论应表述为「**质量更好但更贵，适合复杂多步问题**」，而不是「不值」。

> 保留意见：n=8 仍偏小，且判官本身有噪声（0.1~0.2 的差异可能部分来自判官）。
> 要下强结论需要扩样并做重复测量。

### 8.2 检索消融（规划通道现在真正生效）

| 配置 | hit@5 | MRR |
|---|---|---|
| none(纯向量) | 0.625 | 0.4000 |
| default(含改写) | 0.750 | 0.5625 |
| default-改写(关) | 0.750 | 0.5417 |
| +graph | 0.750 | 0.5042 |
| +graph+rerank | 0.750 | 0.6250 |
| all(+rerank:rrf) | 0.750 | 0.6042 |

### 8.3 改写通道：三次测量互相矛盾 → 判定为「无法与噪声区分」

| 测量 | 配置对比 | Δ MRR |
|---|---|---|
| 首轮（规划部分生效）| default vs default-改写(关) | +0.135 |
| 第二轮（确定性，无规划）| routing+keywords vs +rewrite | **−0.006** |
| 第三轮（规划生效）| default vs default-改写(关) | +0.021 |

三次 Δ 分布在 −0.006 ~ +0.135 之间，而 8 例评测集里**1 个用例翻转 = 0.125 hit@5 / 约 0.1 MRR**——
也就是说这些差异全在噪声带内。**结论：现有证据不支持「改写通道有增益」，也无法证明它有害。**

同理，规划通道的效果（0.5104 → 0.5417，Δ≈+0.03）也在噪声带内，不能据此断言有效。

**这直接指向一个方法学结论：8 条评测例分辨率不足**，任何小于 ~0.1 的差异都不可判读。
在扩样（建议 ≥50 条）之前，不应再基于单次消融调整默认开关。



## 九、下一步

| 优先级 | 事项 |
|---|---|
| **P0** | **扩评测集到 ≥50 条**：当前 8 条无法判读任何 <0.1 的差异，所有开关决策都被卡在这里 |
| P0 | 解决规划通道的可复现性（缓存/固定 plan 输出），否则含规划的消融两次不可比 |
| P1 | Agent 更优但更贵 → 补**成本剖面**（平均步数 / LLM 调用数 / P50-P95 延迟），才能回答"值不值" |
| P1 | 判官需要用人工标注校准（20~30 条）后再看 0.1 量级的差异 |
| P2 | 用「同义词命中」指标筛掉词表里拍脑袋加的词 |
| P2 | `#12`（FastAPI 两套文档网址）两种模式都偏低 → 单例深挖，或调整该用例的期望引用 |

